WAYFOUND_API_KEY=your-api-key-here
WAYFOUND_AGENT_ID=your-agent-id-here

# Optional: route subagent sessions to separate Wayfound supervisors.
# Use WAYFOUND_AGENT_ID_<NAME> where <NAME> is the subagent name in
# uppercase with hyphens replaced by underscores.
# If not set, subagent events are inlined into the orchestrator session.
#
# WAYFOUND_AGENT_ID_NEWS_RESEARCHER=your-news-agent-id-here
# WAYFOUND_AGENT_ID_RATINGS_RESEARCHER=your-ratings-agent-id-here
